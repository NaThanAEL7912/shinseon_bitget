# ShinSeon Bitget 기획서_136 (클라이언트 오더플로우 레이더 로그 문구 '신규 강제 청산' 100% 완벽 통일 완공)

## 📌 개요
폐하께서 올려주신 캡처 화면 팩트 분석을 통해, 클라이언트(`shinseon_client.pyw`) 내부의 오더플로우 레이더 독립 타이머(라인 2925)가 기존 문구(`신규 청산`)를 출력하고 있던 스모킹 건 버그를 발견하여 이를 **`💥 [바이낸스 청산포착] LONG/SHORT 신규 강제 청산 $XX,XXX 발생!`** 으로 100% 동일하게 표준화 완공합니다.

---

## 🎯 정밀 수술안

### 1. ✏️ 클라이언트 오더플로우 레이더 로깅 문구 교정
- **원인 팩트:** `shinseon_client.pyw` 라인 2925에 `self.add_log(f"💥 [바이낸스 {tag_str}] {side_label} 신규 청산 ...")` 로직이 남아있어, 화면 하단 로그 창에 '신규 청산'으로 노출되었음.
- **수술 팩트:** 해당 구문을 **`💥 [바이낸스 청산포착] {side_label} 신규 강제 청산 ${delta_liq:,.0f} 발생! (1분 누적: ${liq_10s:,.0f})`** 로 정격 교정하여 서버와 클라이언트 화면 문구를 100% 완벽 통일 완공 (V5.35).

---

## 🛠 수술 및 배포 절차 (V5.35)
- [x] 1. **`shinseon_client.pyw` 수술** (개발 완료)
   - 라인 2925 로그 문구 '신규 강제 청산' 완벽 교정 및 버전 `V5.35` 세팅 완료.
- [x] 2. **`shinseon_server.py` 수술** (개발 완료)
   - 버전 `V5.35` 세팅 완료.
- [x] 3. **검증 및 배포 (V5.35)** (개발 완료)
   - AWS 서버 PID 77162 가동 팩트 검증 및 Git 푸시 배포 완공.
