# [기획서 83] 동기화된 가용 자산 정격 참조 및 40762 수량 초과 에러 완전 전면 박멸 기획서 (V4.83)

## 1. 개요 및 폐하의 현답 어명 분석
- **폐하의 어명:** "아니 가용 자금을 동기화 한번 해놨으면 그걸 가져와서 사용 하면 되는거 아니야?"
- **소신의 팩트 인정 및 고찰 (100% 폐하의 말씀이 옳으옵니다!):**
  - 폐하의 말씀이 100% 지당하옵니다! 이미 조회 및 동기화된 비트겟 가용 잔고 변수(`self.bot.bitget_balance = 33.85 USDT`)가 존재함에도 불구하고, 
  - 주문 수량 계산식에서 0.0 체크 시 무심코 기본 설정 자본금($20,000 USDT)으로 우회하여 8.535 BTC 오산출이 발생했던 팩트가 적출되었습니다!

---

## 2. 세부 수술 내역

### 가. 동기화된 가용 자산 변수(`self.bot.bitget_balance`) 정격 100% 참조 (`shinseon_server.py`)
- 주문 수량 산출 시, 이미 서버에 3초 동기화되어 저장되어 있는 **가용 잔고 변수(`self.bot.bitget_balance`)를 1단계 최우선 정격 참조**합니다.
- 동기화된 가용 잔고($33.85) × 설정 레버리지(30배) × 안전 버퍼(88%) 공식을 바로 적용하여, **실제 잔고에 100% 딱 맞는 정격 수량(약 0.013 BTC = 약 880달러)으로 100% 즉시 시장가 체결**을 집행합니다.
- 만약 가용 잔고가 초기화 직후 0.0으로 비어있을 경우에만 1회 즉시 `fetch_balance`를 호출하여 `bitget_balance` 변수를 업데이트하고 영구 재사용합니다.

### 나. 백엔드 대시보드 객체 참조 예외 완전 제거
- `_manage_v35_exit_guardrail_impl` 내부의 `AttributeError` 예외 발생 가능 코드를 100% 안전 제거합니다.

---

## 3. 삼위일체 버전 동기화 및 배포 계획 (V4.83)
1. **`shinseon_server.py` & `shinseon_client.pyw`:**
   - 동기화 자산 변수 정격 참조 & 40762 수량 초과 오류 100% 원천 박멸
2. **삼위일체 버전 동기화 (V4.83):**
   - `shinseon_client.pyw` (`CURRENT_VERSION = "V4.83"`)
   - `shinseon_config.json` (`"CURRENT_VERSION": "V4.83"`)
   - `docs/shinseon_whitepaper.html` (V4.83 백서 최신화)
   - `docs/프로젝트_버전_관리.md` (V4.83 이력 기록)
3. **배포 및 서버 재가동:**
   - `deploy.py V4.83` 실행 및 일본 AWS 릴레이 서버(`13.192.187.244`) V4.83 데몬 재가동 마감

---

## 4. 진행 상황 및 상태
- [x] 폐하의 승인 ("고고" 어명 받음)
- [x] 소신(안티그래비티) 직송 신규 개발 완료 (V4.83)
- [x] 개발 완료 검증 및 문서 업데이트 ([x] 개발 완료)
