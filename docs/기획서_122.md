# ShinSeon Bitget 기획서_122 (C:\Working\SHINSEON 원본 기준 세션/추매/불타기 알고리즘 대조 및 수술서)

## 📌 개요
`C:\Working\SHINSEON` 원본 소스코드를 1:1 정밀 역공학 분석하여, **태평양 세션을 포함한 4대 세션 시간 및 임계치**, **동일 신호 재발생 & 마이너스 도달 기반 2차/3차 추가 매수(물타기)**, 및 **1차 익절 후 눌림목 동일 신호 불타기(Pyramiding)** 알고리즘을 비트겟 프로젝트에 100% 동일하게 맞춰 수술하는 계획을 정립합니다.

---

## 🔍 [C:\Working\SHINSEON 원본 vs 현재 비트겟 대조 분석 팩트]

### 1. 🌐 4대 세션 분류 및 KST 시간/임계치 대조
| 세션명 | KST 구분 시간 (1분 완충 타임락) | 1분 청산 임계치 | 1분 OI속도 임계치 | 손절선(SL) | 주말 대응 |
|---|---|---|---|---|---|
| **🔴 아시아 장세** | `08:59:00 ~ 15:58:59` | $100,000 | ±0.12% | -0.5% | `weekend_asia` 지원 |
| **🟡 유럽 장세** | `15:59:00 ~ 22:28:59` | $100,000 | ±0.15% | -0.5% | `weekend_europe` 지원 |
| **🟢 미국 본장** | `22:29:00 ~ 04:58:59` | $300,000 | ±0.20% | -1.3% | `weekend_us` 지원 |
| **⚪ 태평양 횡보 (추가)** | `04:59:00 ~ 08:58:59` | $50,000 | ±0.09% | -0.3% | `weekend_pacific` 지원 |

> **수정할 점:** 비트겟 프로젝트에서 누락되었던 **태평양 세션(04:59~08:58)**을 전격 탑재하고, 1분 완충 타임락 KST 세션 구분 시간(08:59 / 15:59 / 22:29 / 04:59)으로 정밀 교정합니다.

---

### 2. ⚡ 2차 / 3차 추가 매수 (동일 신호 & 마이너스 하락폭) 알고리즘 대조
- **원본 규칙:**
  1. 포지션 보유 중 **진입 방향과 동일한 타점 시그널(LONG or SHORT)이 재발생**해야만 검증 시작.
  2. 현재 시세가 **1차 진입가 대비 설정된 손실 폭(`split_entry_2_trigger_pct`, 기본 -0.3%)보다 더 마이너스로 하락/상승**했을 때만 2차 추가 매수 발주!
  3. 2차 진입 후, 다시 **동일 시그널이 재발생**하고 **1차 진입가 대비 설정된 3차 손실 폭(`split_entry_3_trigger_pct`, 기본 -0.6%)보다 더 마이너스**일 때 3차 추가 매수 발주!
  4. 추가 매수간 쿨다운 타임(`split_cooldown_seconds`, 기본 900초) 적용.

> **수정할 점:** 기존의 단순 틱 하락 발주 로직을 걷어내고, **"동일 방향 타점 신호 재발생 + 1차 평단 대비 -0.3% / -0.6% 이상 마이너스 손실 도달 + 쿨다운 경과"** 3대 조건 충족 시에만 2차/3차 추매가 나가도록 수술합니다.

---

### 3. 🔥 추세 추종 불타기 (Pyramiding) 알고리즘 대조
- **원본 규칙:**
  1. `pyramiding_enabled = True` 설정 시 가동.
  2. 1차 분할 익절(+0.60% 도달 50% 청산) 완료 후, 가격이 눌림목/풀백(기본 +0.3% 지점)으로 내려왔을 때.
  3. **진입 방향과 동일한 타점 시그널이 재발생하면** 설정된 불타기 비중(`pyramiding_ratio`, 기본 30%)으로 시장가 추가 불타기 발주!

> **수정할 점:** 1차 익절 완료 후 동일 방향 시그널 재발생 시 불타기가 발주되는 추세 추종 불타기 엔진을 서버 `shinseon_server.py` 및 `core_logic.py`에 완전 이식합니다.

---

### 4. ⏳ 익절 (15초) & 손절 (300초) 쿨타임 선제 락킹 교정
- **원본 로직 (`SHINSEON`):**
  1. 청산 발주 비동기 처리(`await`)가 타기 전 **미리 선제 락킹(`self.cooldown_until_time = time.time() + profit_cd`)을 즉시 부여**하여 0.001초 틈새 휩소 이중 주문 100% 원천 차단!
  2. 청산 완료 및 PnL 판정 시:
     - 청산 사유가 **"손절"**이거나 PnL이 **음수(손실)**인 경우 ➡️ **손절 쿨타임 300초 (5분)** 정격 할당.
     - 청산 사유가 **익절**인 경우 ➡️ **익절 쿨타임 15초** 정격 할당.
  3. 쿨타임 카운트다운 타이머가 도는 동안은 어떠한 저격 시그널이 와도 진입 수문장에서 0.001초 만에 100% 거부.

> **수정할 점:** 비트겟 거래소 주문 체결 지연 시 발생하던 쿨타임 누수를 막기 위해, 청산 격발 시점 **선제 락킹(Pre-locking)** 및 **손절(300초)/익절(15초) PnL 기반 정격 할당 구조**로 완전 통합 교정합니다.

---

### 5. ⏱️ 신규 진입 완료 시 무조건 60초(1분) 쿨타임 가동
- **원본 로직 (`SHINSEON`):**
  1. 신규 1차 진입 주문 체결 및 성공 즉시 **`cooldown_until_time = time.time() + 60.0` (60초 쿨타임)** 격발!
  2. 진입 후 60초(1분) 동안은 동일 스파이크 신호가 지속 도달하더라도 **"신규 진입 60초 쿨타임 대기 중"** 상태로 0.001초 만에 100% 진입 보류 처리.

> **수정할 점:** 신규 진입 체결 완료 직후 **60초(1분) 무조건 쿨타임 엔진**을 전격 장착하여 연속 중복 진입을 100% 원천 방지합니다.

---

### 6. 🛡️ OI 감소 시 반대 신호 페이크(Fake) 차단 필터링 엔진
- **원본 로직 (`SHINSEON`):**
  1. 포지션 보유 중 대량 청산 스파이크로 인해 미결제약정(OI)이 급격히 빠질 때(`oi_delta_1m < 0`), 반대 방향 잔상 신호가 발생하더라도 이는 신규 자금 유입이 아닌 **"OI 감소성 단순 청산 페이크 신호"**로 즉시 간주!
  2. 반대 신호 청산 스위칭 발동 3대 엄격 조건:
     - **`oi_delta_1m > 0`** (반드시 신규 자금이 들어오는 OI 플러스 상태)
     - **`rolling_1m_liq_usd >= target_liq`** (세션 청산액 임계치 이상)
     - **`abs(oi_delta_1m) >= target_oi`** (세션 OI 속도 임계치 이상)
  3. OI가 감소하고 있는 마이너스 상태(`oi_delta_1m <= 0`)에서 찍히는 반대 신호는 100% 페이크로 0.001초 만에 무시 및 차단하여 보유 포지션을 안전하게 유지 보존함.

> **수정할 점:** 반대 청산 스위칭 조건에 **"OI > 0 플러스 신규 자금 유입 필터"**를 100% 완벽 적용하여 단순 청산에 의한 OI 감소성 페이크 반대 신호를 100% 원천 걸러냅니다.

---

### 7. ⚖️ LONG / SHORT 판단 기준 100% 원본 일치 검증
- **원본 로직 (`SHINSEON`):**
  - **`LONG` (매수 저격):** 숏 청산 폭등 (`short_liq >= long_liq`) **AND** 1분 OI 속도 상승 (`oi_delta_1m >= +target_oi`) **AND** 1분 청산액 돌파 (`rolling_1m_liq_usd >= target_liq`)
  - **`SHORT` (매도 저격):** 롱 청산 폭락 (`long_liq > short_liq`) **AND** 1분 OI 속도 하락 (`oi_delta_1m <= -target_oi`) **AND** 1분 청산액 돌파 (`rolling_1m_liq_usd >= target_liq`)

> **검증 결과:** 현재 `shinseon_server.py`에 구축된 LONG/SHORT 저격 판단 알고리즘은 `C:\Working\SHINSEON` 원본 기준과 **100% 완전히 동일함**을 팩트로 검증 완료하였습니다.

---

## 🛠 수술 및 배포 절차 (V5.22)
- [x] 1. **`shinseon_server.py` & `shinseon_config.json` 수술** (개발 완료)
   - 4대 세션(태평양 포함) KST 시간대 및 세션별 임계치 정격 교정 완료.
   - 동일 신호 재발생 + 마이너스 손실폭(-0.3% / -0.6%) 기반 2차/3차 추매 엔진 교정 완료.
   - 1차 익절 후 동일 신호 눌림목 불타기(Pyramiding) 엔진 완벽 탑재.
   - 청산 전 선제 락킹 및 익절(15초)/손절(300초) 쿨타임 완벽 연동.
   - 신규 진입 성공 즉시 60초(1분) 무조건 쿨타임 가동 엔진 탑재.
   - OI 감소성 페이크 반대 신호 필터링(`oi_delta_1m > 0` 검증) 완벽 탑재.
- [x] 2. **`shinseon_client.pyw` UI 수술** (개발 완료)
   - 세션 설정 및 불타기/추매/쿨타임 설정 UI를 `C:\Working\SHINSEON` 원본과 100% 동일하게 일치 완료.
- [x] 3. **검증 및 배포 (V5.22)** (개발 완료)
   - AWS 서버 PID 74113 가동 팩트 검증 및 Git 푸시 배포 완공.
