# [기획서_370] 기획서 369 삼위일체 3대 AND 공식 28일/29일 실측 CSV 전수 재연산 및 클린 파일 생성 기획서

## 1. 개요 및 배경
- **문서명**: `기획서_370_기획서369_삼위일체3대AND_28일29일_실측CSV_전수재연산_및_클린파일생성_기획서.md`
- **목적**: 
  1. `기획서_369`에서 확립된 **'삼위일체 3대 AND 포용 동조(5s, 1m, Slope >= 0 또는 <= 0)'** 공식을 8월 28일 및 8월 29일 실측 CSV 데이터에 전수 대입하여 재연산함.
  2. 과거 구형 로직의 오류(18:05의 가짜 숏 등)를 완벽히 바로잡은 **클린 마스터 파일(TRIPLE_CLEAN.csv)**과 엑셀 검토용 **신호 추출본(TRIPLE_SIGNALS_ONLY.csv)**을 신규 생성함.
- **적용 버전**: `V7.72`
- **수행 상태**: [x] 개발 완료 (폐하의 '고고' 어명 하에 완공)

---

## 2. 생성 대상 파일 목록

1. **8월 28일 전수 재연산 파일**:
   - 전체 85,830행 풀 데이터: `downloads/2026-08-28/orderflow_history_2026-08-28_TRIPLE_CLEAN.csv`
   - 신호 발생 행만 추출본 (엑셀용): `downloads/2026-08-28/orderflow_history_2026-08-28_TRIPLE_SIGNALS_ONLY.csv`
2. **8월 29일 전수 재연산 파일**:
   - 전체 풀 데이터: `downloads/2026-08-29/orderflow_history_2026-08-29_TRIPLE_CLEAN.csv`
   - 신호 발생 행만 추출본 (엑셀용): `downloads/2026-08-29/orderflow_history_2026-08-29_TRIPLE_SIGNALS_ONLY.csv`

---

## 3. 핵심 알고리즘 명세 (기획서 369 100% 반영)

```python
# 0단계 필수 임계치
stage0_passed = (rolling_1m_liq >= target_liq) and (abs(oi_speed) >= target_oi)

all_zero = (price_delta_5s == 0) and (price_delta_1m == 0) and (price_slope_1m == 0)

if stage0_passed and not all_zero:
    # 🟢 찐 롱: 5s >= 0 AND 1m >= 0 AND Slope >= 0
    if (price_delta_5s >= 0) and (price_delta_1m >= 0) and (price_slope_1m >= 0):
        signal = "LONG"
    # 🔴 찐 숏: 5s <= 0 AND 1m <= 0 AND Slope <= 0
    elif (price_delta_5s <= 0) and (price_delta_1m <= 0) and (price_slope_1m <= 0):
        signal = "SHORT"
    else:
        signal = "NONE"
else:
    signal = "NONE"
```

---

## 4. 작업 및 배포 계획
1. 서브에이전트 코더를 통해 28일 및 29일 전체 CSV 데이터에 기획서 369 공식을 대입하여 4개의 클린 CSV 파일 생성 (`utf-8-sig` 인코딩).
2. 특히 28일 18:05:58 구간이 정상적으로 `LONG`으로 교정되었는지 검증.
3. 버전 삼위일체(V7.72) 갱신 및 `docs/프로젝트_버전_관리.md` 이력 적재.
4. AWS 도쿄 실전 서버 배포(`deploy.py V7.72`) 및 GitHub main 백업 푸시.

---

## 5. 완공 및 생성 파일 내역

1. **8월 28일 재연산 완료 파일**:
   - `downloads/2026-08-28/orderflow_history_2026-08-28_TRIPLE_CLEAN.csv` (총 85,830행, utf-8-sig)
   - `downloads/2026-08-28/orderflow_history_2026-08-28_TRIPLE_SIGNALS_ONLY.csv` (신호 78행 [SHORT 52, LONG 26], utf-8-sig)
   - 🎯 **18:05:58 교정 확인**: Row 64588 `Signal=LONG` (기존 구형 SHORT 오작동 완벽 박멸 및 롱 정상 포착)

2. **8월 29일 재연산 완료 파일**:
   - `downloads/2026-08-29/orderflow_history_2026-08-29_TRIPLE_CLEAN.csv` (총 50,315행, utf-8-sig)
   - `downloads/2026-08-29/orderflow_history_2026-08-29_TRIPLE_SIGNALS_ONLY.csv` (신호 671행 [LONG 428, SHORT 243], utf-8-sig)

