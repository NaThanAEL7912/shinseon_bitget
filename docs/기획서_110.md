# ShinSeon Bitget 기획서_110

## 📌 개요
마스터 대시보드 상단 헤더 레이아웃의 누적 실적 영역을 우측 패널 경계선 근처로 우측 정렬하고, **[📊 누적 상세 보고서] 버튼을 누적 실적 라벨의 왼쪽(앞쪽)**에 오도록 순서 및 정렬 레이아웃을 개편합니다.

---

## 🎯 UI 레이아웃 개편 항목
- [x] 1. **누적 실적 요소의 위치 및 순서 변경** (개발 완료)
  - 배치 순서: `[브랜드 타이틀]` ➡️ `[여백 밀어내기]` ➡️ `[📊 누적 상세 보고서 버튼]` ➡️ `[🏆 누적 실적 라벨]` ➡️ `[📅 금일 실적 라벨]`
  - 누적 실적 그룹 전체가 차트 패널 우측 경계선 부근으로 깔끔하게 우측 정렬되도록 레이아웃 조정.
- [x] 2. **클라이언트 구문 검증 및 푸시 (V5.10)** (개발 완료)
  - `py_compile` 검증 후 `python deploy.py V5.10` 배포.
