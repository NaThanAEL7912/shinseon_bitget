# 🏛️ [기획서 318] 백테스터 설정 저장 시 실전 봇 파일 오염 방지 및 완전 격리 수술

> **문서 번호:** `기획서_318`  
> **버전:** `V7.28`  
> **작성 일시:** 2026-08-24 15:04:00 KST  
> **완료 일시:** 2026-08-24 15:05:30 KST  
> **상태:** ✅ **[완전 격리 수술 및 실전 설정 원상 복구 완료]**

---

## 1. 🔍 문제점 원인 분석 및 완치 결과

- **완치 결과:**
  1. `shinseon_backtester.pyw`에서 실전 봇 파일(`shinseon_config.json`)을 덮어쓰던 위험 코드를 **100% 영구 삭제**함.
  2. 이제 백테스터에서 [설정 저장] 버튼을 눌러도 **오직 `shinseon_backtest_config.json`에만 안전하게 저장**되며, 실전 봇 파일(`shinseon_config.json`)에는 단 1바이트도 영향을 주지 않는 완전한 방화벽이 구축됨.
  3. 실전 봇 설정(`shinseon_config.json`)은 Git 정규 기록(HEAD)으로부터 **100% 무결한 오리지널 실전 값으로 완벽 복구 완료**됨.

---

## 2. 📋 원상 복구된 실전 봇 핵심 설정값 확인표

- **아시아 세션 (ASIA):** 청산 `$25만` / OI `0.05` / 손절 `-0.60%` / 1차익절 `+0.80%` / 2차익절 `+1.20%` / 본전가드 `+0.50%`
- **유럽 세션 (LONDON):** 청산 `$60만` / OI `0.18` / 손절 `-0.80%` / 1차익절 `+1.00%` / 2차익절 `+1.20%` / 본전가드 `+0.50%`
- **미국 본장 (NY):** 청산 `$65만` / OI `0.18` / 손절 `-1.30%` / 1차익절 `+1.20%` / 2차익절 `+1.50%` / 본전가드 `+1.00%`
- **태평양 세션 (PACIFIC):** 청산 `$50만` / OI `0.12` / 손절 `-1.00%` / 1차익절 `+0.40%` / 2차익절 `+0.60%` / 본전가드 `+0.20%`