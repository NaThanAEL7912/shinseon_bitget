# [기획서_372] 백테스터 엔진 0나누기 오류(ZeroDivisionError) 원천 방어 및 정통 청산 모드 UI 동기화 기획서

## 1. 개요 및 배경
- **문서명**: `기획서_372_백테스터_엔진_0나누기_오류_원천방어_및_정통청산모드_UI동기화_기획서.md`
- **목적**: 
  1. 백테스터 실행 시 발생한 `float division by zero` (0으로 나누기 오류)의 발생 지점(분할 익절 비율 `1.0 - tp1_split_ratio`, 매수 비중 합산, 피크 자산 대비 MDD 연산 등)을 전수 찾아내어 **`max(0.0001, ...)` 및 안전 조건문으로 100% 원천 방어**함.
  2. 백테스터 GUI(`shinseon_backtester.pyw`)의 기본 세션 임계치(아시아 $30만/0.12%, 유럽 $60만/0.12%, 미국 $100만/0.15%) 및 가드레일 설정을 **'삼위일체 3대 AND 정통 청산(Close to Flat)'** 표준으로 동기화함.
- **적용 대상**: `backtest_engine.py`, `shinseon_backtester.pyw`, `shinseon_server.py`, `shinseon_client.pyw`, `docs/shinseon_whitepaper.html`
- **목표 버전**: `V7.74`
- **수행 상태**: [x] 개발 완료 (2026-08-29)

---

## 2. 0나누기 오류(ZeroDivisionError) 전수 방어 설계

```python
# 1. 분할 익절 잔여 PnL 연산 안전 방어
actual_pnl = fp / max(0.0001, (1.0 - tp1_split_ratio)) if is_tp1 else fp

# 2. 물타기 및 불타기 평단가 연산 안전 방어
ep = (ep1 * t_buy1 + ep2 * t_buy2) / max(0.0001, (t_buy1 + t_buy2))
ep = (ep1 * t_buy1 + ep2 * t_buy2 + ep3 * t_buy3) / max(0.0001, (t_buy1 + t_buy2 + t_buy3))
ep = (rem_lev * ep + pyra_lev * cp) / max(0.0001, new_lev)

# 3. MDD 및 승률, ROI 연산 안전 방어
max_drawdown_pct = (dd / peak_equity * 100.0) if peak_equity > 0 else 0.0
win_rate = (wins / total_trades * 100.0) if total_trades > 0 else 0.0
roi = (net / initial_balance * 100.0) if initial_balance > 0 else 0.0
profit_factor = (total_win_amt / total_loss_amt) if total_loss_amt > 0 else (999.99 if total_win_amt > 0 else 0.0)
```

---

## 3. 백테스터 GUI (`shinseon_backtester.pyw`) 기본값 동기화
1. **1번 탭 (세션별 임계치 기본값)**:
   - 아시아: `$300,000` / `0.12%` / 손절 `-0.60%`
   - 유럽: `$600,000` / `0.12%` / 손절 `-0.80%`
   - 미국: `$1,000,000` / `0.15%` / 손절 `-0.80%`
   - 태평양: `$400,000` / `0.12%` / 손절 `-0.60%`
2. **3번 탭 (가드레일)**:
   - `중간 수익 보존 가드` 및 `2시간 무위험 가드` 기본 OFF 설정 (대세 파동 홀딩 보장).

---

## 4. 작업 및 배포 계획
1. `backtest_engine.py` 내 모든 나눗셈 연산 안전 래핑 및 예외 처리.
2. `shinseon_backtester.pyw` UI 기본값 갱신.
3. 버전 삼위일체(`V7.74`) 갱신 및 `docs/프로젝트_버전_관리.md` 이력 등재.
4. AWS 도쿄 실전 서버 배포 및 GitHub main 백업 푸시.
