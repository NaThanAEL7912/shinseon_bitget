# [기획서 41] 실시간 레이더 로그 중복 출력 제거 및 단일행 정갈화 (V4.44)

## 1. 개요 및 트레이스 백 정밀 팩트 원인 분석
- **폐하의 지적 사항:**
  `[04:29:22]` 로그에 동일한 실시간 시세/PnL 상태가 2줄로 겹쳐서 도배 출력되는 현상에 대한 단일행 정량화 요청 (로그 제출)
- **소신의 정밀 트레이스 백 100% 팩트 원인 밝혀냄:**
  1. `shinseon_client.pyw` 코드 분석 결과, 실시간 파라미터 패킷(`ui_update`)을 받으면 **웹소켓 핸들러(라인 1252)와 UI 갱신 루프(`update_live_ui`, 라인 2525) 2곳에서 동시에 `add_log()`를 중복 격발**하고 있었던 원인을 발견하였습니다.
  2. **원인 (중복 로깅 이원화 맹점):**
     - 웹소켓 수신 시 raw 메시지(`[LONG 진입 ...]`)를 1번 찍고,
     - 0.001초 직후 `update_live_ui()`에서 정갈하게 포매팅된 `레이더 스캔 ➡️ [LONG 진입 ...] (청산: $..., OI: ...)`를 또 1번 찍어 매초 2줄씩 겹쳐 떴던 것입니다!

---

## 2. 상세 수술 및 원천 복구 계획 (V4.44)

###가. 웹소켓 핸들러 중복 로깅 제거 및 단일행 정갈화 (`shinseon_client.pyw`)
1. **웹소켓 핸들러 라인 1252 중복 `add_log()` 제거:**
   - raw 메시지 2중 출력을 완전 삭제하고, 오직 `update_live_ui()`에서 제공하는 아름다운 단일행 포맷만 정갈하게 출력되도록 수술합니다.
2. **폐하께서 원하시는 단일행 출력 확정:**
   - `[04:29:22] 레이더 스캔 ➡️ [LONG 진입 @ 64,832.6] PnL: +0.12% (+0.0 USDT) (가드레일 도약 대기: +0.90%) (청산: $258,898, OI: -0.005%)` 단 1줄만 깔끔하게 찍힙니다.

###나. 소신(안티그래비티) 직접 수술 및 배포
1. 버전을 `V4.44`로 지정.
2. 소신이 직접 소스코드 중복 로깅 제거 집행.
3. `deploy.py V4.44` 실행하여 Commit & GitHub main push.
4. V4.44 대시보드 구동 후 로그 중복 0건 및 단일행 깔끔 출력 100% 최종 검증.

---

## 3. 진행 상황 및 상태
- [x] 폐하의 승인 ("고고" 어명 받음)
- [x] 소신(안티그래비티) 직송 개발 완료 (V4.44)
- [x] 100% 2차 정밀 검수 완료
- [x] 개발 완료 검증 및 문서 업데이트 ([x] 개발 완료)
