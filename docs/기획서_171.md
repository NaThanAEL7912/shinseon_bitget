# ShinSeon Bitget 기획서_171 (스마트 스탑 -0.9% 미청산 최종 진범 파악 및 100% 완치안)

## 📌 1. 최종 원인 파악 및 팩트 분석 (스마트 스탑 미청산의 진짜 범인)

폐하께서 보내주신 -1.08% ROE 화면과 AWS 서버 텔레메트리 로그를 정밀 수사한 결과, **-0.90% 설정치를 한참 지나 -1.08%, -1.23%까지 떨어져도 청산이 안 되던 결정적 팩트 원인**을 100% 명백하게 밝혀내었습니다!

### 🔍 팩트 원인 분석:
1. **스마트 스탑 감시 배경 테스크(`manage_v35_exit_guardrail`) 미구동 (결정적 진범):**
   - 서버에서 `CMD_SET_SMART_STOP` 패킷을 수신했을 때 변수(`custom_stop_active = True`, `custom_stop_offset_pct = -0.9`)만 설정할 뿐, **실제 감시를 수행하는 비동기 루프 테스크(`manage_v35_exit_guardrail`)를 팝업 구동(create_task)시키는 코드가 빠져 있었습니다!**
   - 텔레메트리 표시 루프에서는 변수 상태만 읽어서 화면에 `(🛡 스마트 스탑 가드: -0.90% ROE 손절 감시 중)` 텍스트를 계속 찍어주고 있었기에 마치 감시 중인 것처럼 보였으나, **정작 뒤에서 조건문을 돌리고 청산 주문을 쏘는 감시 엔진 프로세스 자체가 가동되지 않고 멈춰 있었던 팩트**가 발견되었습니다!
2. **`is_snipe_active` (자동 봇 정지 상태) 및 소수점 조건식:**
   - 감시 루프 내부 L2677 위치에 `if not self.is_snipe_active: continue` 구문이 남아있어 자동 봇 정지 상태에서 감시를 조기 스킵하고 있었습니다.

---

## 🛠️ 2. 정공법 완치 수술 상세 내역

### 2.1. `shinseon_server.py` 수술 계획
1. **`CMD_SET_SMART_STOP` 수신 시 감시 테스크 즉시 자동 팝업 구동:**
   - 서버에서 스마트 스탑 설정 명령(`active = True`)을 받는 즉시, 감시 루프가 돌고 있지 않다면(`if not is_guardrail_running`):
   - `asyncio.create_task(self.bot_core.v35_engine.manage_v35_exit_guardrail(entry_direction))`를 즉시 호출하여 24시간 실시간 감시 엔진을 강제 부활시킵니다.
2. **스마트 스탑 감시의 독립 최우선 순위 승격:**
   - `manage_v35_exit_guardrail` 내에서 봇 상태(`is_snipe_active`)와 관계없이 포지션 및 `custom_stop_active`가 True이면 0.1초 간격으로 무조건 ROE를 체크하도록 루프 최상단으로 승격합니다.
3. **UI 표시 정밀도 2자리 반올림(`round(..., 2)`) 적용:**
   - `live_roe_rounded = round(live_roe, 2)` 수식을 적용하여 화면에 표시된 ROE가 오프셋 조건(-0.90%)에 도달하는 순간 100% 즉시 시장가 청산을 격발합니다.

---

## 🚀 3. 진행 절차
1. 위 내용대로 `shinseon_server.py` 엔진의 스마트 스탑 감시 팝업 구동부 및 감시 루프를 완치 수술합니다.
2. `deploy.py V5.69` 실행하여 GitHub 원격 배포 및 SCP 전송.
3. SSH를 통해 AWS 서버 데몬 재부팅 후 스마트 스탑 즉시 청산 실전 완치 검증.
4. 완료 후 `docs/프로젝트_버전_관리.md` 이력 업데이트 및 기획서 완료 처리.

폐하, 위 정공법 기획안대로 **스마트 스탑 감시 엔진 강제 부활 및 -0.9% 즉시 청산 100% 완치 수술**을 집행할 수 있도록 승인 어명(**"고고"**)을 내려 주시옵소서! 🙇절
