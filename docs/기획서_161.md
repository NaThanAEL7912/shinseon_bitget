# ShinSeon Bitget 기획서_161 (포지션 보유 중 반대 청산시 듀얼 임계치 >= target_liq & >= target_oi 필수 탑재 완치 완공서)

## 📌 개요
폐하의 명확하신 어명(**"포지션 유지 중일 때도 >= target_liq >= target_oi 이 값은 당연히 통과해야 하는거야"**)에 따라, **포지션 보유 중 반대 청산 및 스위칭 시에도 듀얼 임계치(청산액 $500K 이상 & OI속도 0.18% 이상)가 100% 동시 충족되었을 때만 반대 청산이 발동**되도록 대전제를 수술하는 완치 완공을 집행합니다.

---

## 🔍 [포지션 보유 중 반대 청산 듀얼 임계치 필수 팩트]

### 1. 🛡️ 보유 중 반대 청산 듀얼 임계치 전제 팩트
- **필수 전제:** `rolling_1m_liq_usd >= target_liq` AND `abs(oi_delta_1m) >= target_oi`
- **실행 메커니즘 팩트:**
  - 듀얼 임계치 미달 시 ➡️ 반대 신호가 발생하더라도 **반대 청산 일절 안함 (기존 포지션 철통 홀딩!)**
  - 듀얼 임계치 100% 통과 시 ➡️ `OI < 0` (찌꺼기 무시 홀딩!) / `OI > 0` (진짜 스위칭 전량 익절 청산!)

---

## 🛡️ [SHINSEON V5.61 보유 중 처리 수술 코드 (듀얼 임계치 필수 전제)]

```python
# [SHINSEON V5.61 포지션 보유 중 반대 청산 듀얼 임계치 필수 엔진]
if self.is_position_active:
    # 0단계: 듀얼 임계치 100% 통과 필수 전제 검사
    if rolling_1m_liq_usd >= target_liq and abs(oi_delta_1m) >= target_oi:
        if direction != self.entry_direction:
            if oi_delta_1m < 0:
                # Case 2-2, Case 3-2: 임계치 돌파했으나 OI < 0 (청산 찌꺼기) ➡️ 철통 홀딩!
                pass
            elif oi_delta_1m > 0:
                # Case 2-3, Case 3-3: 임계치 돌파 + OI > 0 진짜 반대 수급 ➡️ 익절 청산 & 15초 쿨타임 스위칭!
                asyncio.create_task(self.execute_bitget_internal_packet(side="CLEAR", order_type="FORCE_MARKET_UNCAPPED"))
    else:
        # 임계치 미달 시 반대 청산 무조건 스킵 ➡️ 기존 포지션 온전히 홀딩!
        pass
```

---

## 🛠 수술 및 배포 절차 (V5.61)
1. **`shinseon_server.py` & `core_logic.py` 수술:**
   - 보유 중 반대 청산 듀얼 임계치 필수 전제 탑재 수술 및 버전 `V5.61` 세팅.
2. **`shinseon_client.pyw` 수술:**
   - 버전 `V5.61` 세팅.
3. **검증 및 배포:**
   - `py_compile` 검증 후 AWS 서버 업로드/데몬 재가동 및 `python deploy.py V5.61` 푸시.
