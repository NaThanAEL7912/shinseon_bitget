# [기획서_356] 서버 텔레메트리 변수명 오류 원천 수정 및 웹소켓 연결 안정화 기획서 (V7.64)

## 1. 개요 및 배경
- 국왕 폐하께서 *"서버가 계속 끊어지는 거 같은데?"*라고 하문하시어 실시간 AWS 실전 서버 로그를 정밀 스캔한 결과:
- `shinseon_server.py`의 1초 틱 텔레메트리 보정 루프에서 `NameError: name 'short_liq_val' is not defined` 에러가 초당 수십 번씩 발생하여 이벤트 루프 병목을 유발하고 클라이언트와의 웹소켓 연결이 간헐적으로 끊어지거나 지연되는 현상이 발견되었습니다.
- 이에 변수명을 100% 정합화하여 에러를 원천 박멸하고 웹소켓 연결을 완벽히 안정화하고자 합니다.

---

## 2. 정밀 원인 분석
- [`shinseon_server.py`](file:///C:/Working/ShinSeon_Bitget/shinseon_server.py) Line 1255~1272:
  - 1분 롤링 청산 볼륨이 `short_liq`, `long_liq` 변수로 계산되어 저장됨.
- Line 1306:
  - 청산 주도권 방향 판정 시 `if short_liq_val >= long_liq_val:`로 잘못 참조되어 매 틱마다 `NameError` 예외가 발생함.

---

## 3. 세부 개발 명세

### 1) [`shinseon_server.py`](file:///C:/Working/ShinSeon_Bitget/shinseon_server.py)
- Line 1306: `if short_liq_val >= long_liq_val:` ➡️ `if short_liq >= long_liq:`로 정밀 수정.
- 버전 `V7.64` 갱신.

### 2) 버전 삼위일체 및 문서 동기화
- [`shinseon_client.pyw`](file:///C:/Working/ShinSeon_Bitget/shinseon_client.pyw): 버전 `V7.64` 갱신.
- [`shinseon_config.json`](file:///C:/Working/ShinSeon_Bitget/shinseon_config.json): `"CURRENT_VERSION": "V7.64"` 갱신.
- [`docs/shinseon_whitepaper.html`](file:///C:/Working/ShinSeon_Bitget/docs/shinseon_whitepaper.html): 마스터 버전 `V7.64 (20260828)` 갱신.
- [`docs/shinseon_whitepaper_20260828.html`](file:///C:/Working/ShinSeon_Bitget/docs/shinseon_whitepaper_20260828.html): 일자스탬프 백서 `V7.64` 동기화.
- [`docs/프로젝트_버전_관리.md`](file:///C:/Working/ShinSeon_Bitget/docs/%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8_%EB%B2%84%EC%A0%84_%EA%B4%80%EB%A6%AC.md): V7.64 이력 추가.

---

## 4. 검증 및 배포 계획
1. `python -m py_compile shinseon_server.py` 문법 검증.
2. `python deploy.py V7.64` 실행하여 AWS 실전 도쿄 서버에 원격 배포 및 데몬 재기동.
3. `tail -f /home/ubuntu/shinseon_stdout.log`로 에러가 0건으로 완전히 사라지고 웹소켓 브로드캐스트가 1초의 끊김 없이 매끄럽게 흐르는지 확인.

---

## 5. 진행 상태 체크
- [x] 1. 기획서 356 작성 및 폐하의 "고고" 어명 대기
- [x] 2. `shinseon_server.py` Line 1306 변수명 `short_liq` / `long_liq` 정합화
- [x] 3. 버전 삼위일체 V7.64 갱신 및 AWS 실전 서버 무중단 배포
- [x] 4. 텔레메트리 에러 소멸 및 웹소켓 연결 안정성 실측 검증

---
*작성일시: 2026-08-28 16:45 (KST)*
*작성자: 수석 과학기술참모 장영실*
