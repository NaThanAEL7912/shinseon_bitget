# 기획서 212: 동적 임계치 신호 엔진 변수명 오기(NameError) 긴급 수술 (V6.10)

## 📌 1. 개요 및 원인 긴급 분석
- **현상:**  
  폐하께서 보고해주신 대시보드 멈춤 현상 (*"임계치값 이상 되면 이렇게 뻗어 버린다. 주문도 안들어가고"*).
- **실제 로그 원인 (Empirical Evidence):**  
  `[ERROR] 텔레메트리 보정 루프 에러: name 'long_liq' is not defined`  
  V6.07 청산 주도 비율 2차 방화벽 구현 시, `check_radar_signal_dynamic()` 함수 내부에서 변수명이 `long_liq_usd` / `short_liq_usd`로 선언되어 있었으나 `long_liq` / `short_liq`로 일시적 오기되어 **임계치 돌파 순간 Python NameError 예외가 발생하여 엔진 스레드가 먹통이 되는 버그**였습니다.

---

## 🛠️ 2. 긴급 수술 완료 내역
1. **`shinseon_server.py` L2371~L2377 변수명 긴급 교정:**
   - `long_liq >= short_liq` ➡️ **`long_liq_usd >= short_liq_usd`** 로 100% 명시적 변수 교정 완공.
2. **py_compile 및 AWS 서버 가동 검증 완료:**
   - 에러 0건 및 Bitget 잔고/포지션 실시간 동기화 정상 복원 완료.

---

## 📋 3. 프로젝트 버전 기록 및 상태
- **수정 파일:** `shinseon_server.py`, `docs/프로젝트_버전_관리.md`, `docs/기획서_212.md`
- **배포 버전:** `V6.10`
- **상태:** ✅ 긴급 긴급 수술 완료 및 AWS 실전 데몬 가동 복원 완료
