# 기획서 218: 신호 없음(direction = None) 시 신규 진입 오발조 차단 및 타점 검증 방화벽 완치 (V6.16)

## 📌 1. 개요 및 팩트 분석
- **현상:**  
  23:57:35 KST에 청산액 $4,387,459 및 OI속도 +0.3569%가 터졌을 때, 4대 매트릭스는 방화벽으로 `direction = None` (신호 없음)을 판정했으나, 시스템이 비트겟 거래소에 `SHORT` 진입을 집행하고 텔레그램 알림에 **`[1차 진입 알림] 방향: None`**으로 오기 표출되었습니다.

- **원인:**  
  `shinseon_server.py` L2654 및 L2080의 2중 팩트 결함 포착:
  1. **진입 트리거 검증 결함 (L2654):**  
     신규 진입 집행 구문 `if not self.is_position_active and self.is_snipe_active:` 에서 **`direction`이 `LONG` 또는 `SHORT`인지 검증하는 필수 조건문(`if direction in ["LONG", "SHORT"]:`)이 누락**되어 있었습니다!  
     이에 따라 `direction = None` 상태임에도 청산액과 OI속도 임계치가 돌파했다는 이유만으로 진입 프로세스로 직행했습니다.
  2. **CCXT 발주 3항 연산자 폴백 결함 (L2080):**  
     `ccxt_side = 'buy' if side == 'LONG' else 'sell'`  
     `side`가 `None`으로 전달되자 `None == 'LONG'`이 `False`가 되어 **폴백 값인 `'sell'`(SHORT)로 비트겟 거래소에 숏 체결 주문**을 날렸던 것입니다!

- **조치:**  
  1. L2654 신규 진입 트리거에 **`if direction and direction in ["LONG", "SHORT"]:`** 필수 방화벽 조건문을 추가하여 `None` 시 신규 진입을 100% 원천 차단합니다.
  2. L2080 CCXT 발주 엔진 상단에 **`if side not in ["LONG", "SHORT"]:` 가드레일을 추가**하여 유효하지 않은 방향 패킷 수신 시 단 1건의 주문도 거래소로 나가지 않도록 2중 안전 장치를 구축합니다.

---

## 🛠️ 2. 작업 상세 내역
1. **`shinseon_server.py` L2654 조건문 강화:**
   - 기존: `if not self.is_position_active and self.is_snipe_active and not self.exit_in_progress:`
   - 수정: `if direction and direction in ["LONG", "SHORT"] and not self.is_position_active and self.is_snipe_active and not self.exit_in_progress:`
2. **`shinseon_server.py` L2080 CCXT 발주 엔진 하드 가드레일 추가:**
   - `if side not in ["LONG", "SHORT"]:` ➡️ 즉시 기각 및 발주 취소.

---

## 📋 3. 프로젝트 버전 및 상태
- **대상 파일:** `shinseon_server.py`
- **배포 버전:** `V6.16`
- **상태:** ✅ 개발 완료 및 AWS 실전 서버(PID 121746) 배포 완공 완료
