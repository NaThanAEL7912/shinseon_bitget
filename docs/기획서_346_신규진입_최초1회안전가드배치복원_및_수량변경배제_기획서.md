# [기획서_346] 신규 포지션 최초 진입 시 즉시 3대 안전가드(TP1/TP2/SL) 자동 발주 복원 및 수량 변경 재배치 배제 기획서

---

## 📌 [문서 정보]
- **문서 번호:** 기획서_346
- **작성 일자:** 2026년 8월 27일 16:38 KST
- **작성자:** 안티그래비티 (수석 개발자)
- **수신:** 국왕 폐하
- **현재 실시간 포지션 팩트:** 🔴 **SHORT 1.9505 BTC** (평단가: $78,820.16 / 마크가: 약 $78,825.7 / 미실현 손익: -$10.80 USDT)
- **진행 상태:** `[x] 개발 완료`

---

## 1. 🎯 [기획 배경 및 문제 원인 정밀 규명]

### 1) 폐하의 어명
- *"아니 금방 포지션 신규 진입했는데 안전가드 왜 안생기냐? 신규는 생기게 해야지 신규 이외에 변경 될때만 제거 하라니까 -_-;; 아 멍청아"*

### 2) 결함 원인 분석
- 이전 V7.50 작업 당시 3초 포지션 감시 루프(`sync_bitget_real_position_status`)에서 `was_inactive`(이전 포지션이 없다가 0 ➡️ 1개 이상으로 신규 진입된 최초 순간) 분기까지 일괄 삭제되었습니다.
- 이로 인해 **신규 포지션(모바일/수동/신규)이 최초로 열렸을 때 3초 감시자가 최초 1회 안전가드(TP1/TP2/SL)를 발주하지 못하는 문제**가 발생했습니다.

---

## 2. 🔍 [정공법 완벽 해결 설계]

### 1) 3초 감시 루프 내 "신규 포지션 최초 진입(0 ➡️ >0)" 판정 정밀 복원
- [`shinseon_server.py`](file:///C:/Working/ShinSeon_Bitget/shinseon_server.py)의 `sync_bitget_real_position_status` 함수 내:
```python
if not active_pos:
    # 포지션 0개 상태: 비활성화 처리
    self.v35_engine.is_position_active = False
    self.v35_engine.position_volume = 0
    self.v35_engine.entry_price = 0.0
    self.v35_engine.entry_direction = ""
    self.v35_engine.last_guarded_pos = {}
else:
    # 포지션이 0개에서 1개 이상으로 '신규 진입'된 최초 순간 감지!
    was_inactive = not self.v35_engine.is_position_active
    self.v35_engine.is_position_active = True
    side_val = (active_pos.get('holdSide') or 'long').upper()
    self.v35_engine.entry_direction = side_val
    e_price = float(active_pos.get('openPriceAvg', 0.0) or active_pos.get('entryPrice', 0.0) or 0.0)
    v_contracts = float(active_pos.get('total', 0.0) or active_pos.get('contracts', 0.0) or 0.0)
    
    if e_price > 0.0:
        self.v35_engine.entry_price = e_price
        self.v35_engine.active_position_entry_price = e_price
    if v_contracts > 0.0:
        self.v35_engine.position_volume = v_contracts
        self.v35_engine.position_volume_btc = v_contracts
        
    # 🎯 [신규 진입 최초 1회에만 안전가드 발주!] (물타기/일부익절 등 수량 변경 시에는 절대 재배치 안 함!)
    if was_inactive and e_price > 0.0 and v_contracts > 0.0:
        logger.info(f"🛡️ [신규 포지션 최초 진입 감지] ({side_val} {v_contracts} BTC @ ${e_price:,.1f}) ➡️ 3대 TP/SL 안전가드 즉시 1회 선제 배치 가동!")
        asyncio.create_task(self.v35_engine.place_bitget_tpsl_plan_orders(e_price, side_val, v_contracts))
```

### 2) 현재 실시간 숏 포지션(1.9505 BTC @ $78,820.16)에 대한 3대 안전가드 즉시 발주
- 기획서 승인 즉시 현재 포지션에 대해 세션 기본 3대 안전가드(TP1 50% / TP2 50% / SL 100%)를 거래소에 즉시 발주 집행.

---

## 3. 🛠️ [수정 대상 파일]
1. [`shinseon_server.py`](file:///C:/Working/ShinSeon_Bitget/shinseon_server.py) - `was_inactive` 신규 진입 최초 1회 발주 로직 정밀 복원
2. [`shinseon_client.pyw`](file:///C:/Working/ShinSeon_Bitget/shinseon_client.pyw) - 버전 V7.54 동기화
3. [`shinseon_config.json`](file:///C:/Working/ShinSeon_Bitget/shinseon_config.json) - 버전 V7.54 동기화
4. [`docs/shinseon_whitepaper.html`](file:///C:/Working/ShinSeon_Bitget/docs/shinseon_whitepaper.html) - 백서 V7.54 동기화
5. [`docs/프로젝트_버전_관리.md`](file:///C:/Working/ShinSeon_Bitget/docs/%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8_%EB%B2%84%EC%A0%84_%EA%B4%80%EB%A6%AC.md) - V7.54 이력 적재
6. AWS 도쿄 실전 서버(`13.192.187.244`) 원격 무중단 배포

---

## 4. 📜 [황실 헌법 준수 및 어명 대기]
- 황실 헌법에 따라 본 수정 기획서를 먼저 상주드리오며, 폐하께서 확인 후 **"고고"**라고 어명을 내려주시면, 즉시 쫄다구 코더 에이전트를 가동하여 코드를 완벽히 수정 및 AWS 서버에 배포하고, 현재 숏 포지션에 안전가드를 즉시 배치하겠습니다!
