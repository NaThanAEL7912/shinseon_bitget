# ShinSeon Bitget 기획서_158 (비트겟 격리 isolated 마진모드 100% 강제 수술 및 400172 완치 완공)

## 📌 개요
폐하의 어명(**"우리 주문을 격리로 주문 넣어야 하는데"**)에 따라, **비트겟 거래소의 모든 신규 진입 및 청산 주문의 마진 모드를 `isolated` (격리 모드)로 100% 강제 적용**하고, SHORT 청산 시 `close_side: "buy"` 오탈자 및 400172 에러를 완벽 완치하는 정공법 완치 수술을 집행합니다.

---

## 🔍 [폐하의 어명 & 정밀 완치 수술안]

### 1. 🔍 격리 마진 모드 (`isolated`) 100% 강제 수술 팩트
- **비트겟 v2 API 파라미터 규격:**
  - **`marginMode`**: **`"isolated"`** (격리 마진 모드 전용!)
  - 신규 진입(`open_long` / `open_short`) 및 50% 분할 익절 / 전량 청산시 `marginMode: "isolated"` 를 100% 강제 명시하여, 계정의 격리 레버리지와 1:1 완벽하게 똑같이 주문이 집행되도록 수술함!

### 2. 🔑 SHORT 청산 `close_side: "buy"` 정격 수술 (V5.58)
- **수술 팩트 (V5.58):**
  - **SHORT 포지션 청산시 `close_side: "buy"` (매수 청산)**, **LONG 포지션 청산시 `close_side: "sell"` (매도 청산)** 으로 100% 정격 교정하여 400172 에러 원천 제거!
  - 50% 분할 익절 주문이 비트겟 거래소에서 **`00000` (성공 체결) 되었을 때만 텔레그램 분할 익절 알림이 발송**되도록 완치 완공 (V5.58).

---

## 🛠 수술 및 배포 절차 (V5.58)
- [x] 1. **`shinseon_server.py` 수술** (개발 완료)
   - `marginMode: "isolated"` 전면 강제 적용 완료.
   - SHORT 청산 시 `close_side: "buy"` 오탈자 정격 수술 및 400172 에러 원천 제거 완료.
   - 비트겟 `00000` 체결 성공 팩트 확인 후 텔레그램 발송 연동 완료.
   - 버전 `V5.58` 세팅 완료.
- [x] 2. **`shinseon_client.pyw` 수술** (개발 완료)
   - 버전 `V5.58` 세팅 완료.
- [x] 3. **검증 및 배포 (V5.58)** (개발 완료)
   - AWS 서버 PID 87957 가동 팩트 검증 및 Git 푸시 배포 완공.
